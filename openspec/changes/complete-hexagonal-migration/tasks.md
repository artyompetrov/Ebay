## 1. Infrastructure helpers with no DB/Identity coupling

- [x] 1.1 **Sequencing correction (verified by grep, no file moved yet):** none of `DictionaryExtensions`, `EnumerableExtensions`, `ExceptionExtensions`, `HtmlUtilities`, `ModelsExtensions` has a caller that already lives in the target shape - every actual (only) caller is itself still-legacy code owned by a later phase (`DictionaryExtensions` -> `ConditionExtractor`/`PcsExtractor`/`TestStateExtractor`/`ShippingRatesService`, phase 2; `EnumerableExtensions.Batch` -> `MeasurementPlotWarmupHostedService`, phase 5.4; `ExceptionExtensions.IsNotIntendedCancellation` -> `BackgroundTask.cs`, phase 5.6/7.6; `HtmlUtilities` -> `LotDataToExtract.cs`, phase 6.3; `ModelsExtensions` -> `EbayControllerImplementation`, phase 6.3). Moving any of them now would mean guessing a final home before the phase that actually decides it (e.g. whether the `LotDataExtractor` rules land in `Server.Domain` or `Server.Application.New`). Each helper is relocated in the same commit as its real caller instead; the phase tasks below (2.1, 2.2, 5.4, 5.6, 6.3) now say so explicitly.
- [x] 1.2 **Sequencing correction (same reasoning as 1.1):** `SvgMerger`'s only caller is `MeasurementPlotService`, itself moved in phase 3.4 - relocate `SvgMerger` alongside it there instead of guessing its home now.
- [x] 1.3 Grep every call site of `TransactionScopeFactory` and record the list in this task's PR description (do **not** delete or relocate it yet): expect exactly `EbayControllerImplementation.cs` (x3, phase 6), `SaleAdvertisementCleanupBackgroundTask.cs` (phase 5), `ChipfindBackgroundTask.cs` (phase 5), `CalculatePricesForLotConsumer.cs` (phase 4) - confirm this list is still accurate before relying on it in later phases; each phase above removes its own `TransactionScope` usage once its call site commits through a single `WriteModelDbContext` save, and phase 7 deletes the factory once all 5 are cleared.
- [x] 1.4 Leave `Infrastructure/BackgroundTask.cs`, `WellKnown.cs`, `ServiceCollectionExtensions.cs`, `DatabaseConcurrentAccessSemaphore.cs`, `EbayServerOptions.cs`, `ImageCacheOptions.cs` in place for now - they are relocated by the phases that actually consume them (5 and 7 below); verify none of them are silently forgotten by re-grepping for their usages at the start of section 7.

## 2. LotDataExtractor and GeoIp (self-contained, no aggregate or table involvement)

- [x] 2.1 Move the `LotDataExtractor` parsing rules (`ConditionExtractor`, `PcsExtractor`, `TestStateExtractor`, `ExtractorBase`, `IExtractor`, `ExtractFrom`, `ExtractionResult`) into `Server.Domain/LotDataExtraction` - **revised after PR review**: these regex-to-category mappings (e.g. "never used" -> New, "for parts" -> NotWorking) encode the business meaning of listing phrases, which is domain policy, not application orchestration, so they moved to `Server.Domain` rather than `Server.Application.New` as first landed; `Server.Domain.LotDataExtraction.LotFieldExtractors` exposes them as `IExtractor` only (concrete extractor classes stay `internal`), so `Server.Application.New/LotDataExtractor/ManualFieldsExtractor` still owns the orchestration (looping over extractors, collecting results) as the use-case layer should. `LotTextFields`/`ExtractionResult`/`ExtractFrom` moved to `Server.Domain.LotDataExtraction` too (the extractors' public signature). `DictionaryExtensions.AppendOrCreateNewCollection` (from `Server.Application/Infrastructure`, see 1.1) moved to `Server.Domain` (shared with the Shipping module below, avoiding the duplication the first pass introduced). `WellKnown.Categories.Conditions`/`TestState` (the condition/test-state vocabulary) moved to `Server.Domain/LotCategories.cs`, since it's domain-owned vocabulary for the not-yet-migrated `Lot` aggregate rather than legacy plumbing - `EbayControllerImplementation` now reads it from there too instead of the legacy `WellKnown`; verified with the existing `Tests.Explicit/LotsExplicitTests.cs` (an explicit/live-backend suite, moved onto the new `LotTextFields` input type - could not run it in this environment since it requires a live backend + `EBAY_HELPER_REMOTE_HOST`, but it builds).
- [x] 2.2 **Correction, twice over:** `ShippingRatesService` turned out to hold a static hardcoded rate table with no actual HTTP call (the original task description's "HTTP-calling implementation" was inaccurate) - **revised after PR review**: since it owns the tariff table and the weight-bracket generation formula (business/reference policy) and has zero I/O to adapt, it does not belong behind a driven port/adapter at all; it moved to `Server.Domain/Shipping` as a plain static `ShippingRatesTable` (mirroring `LotCategories`'s role as domain reference data), and the `Server.Adapters.Driven.ShippingRates` project/`IShippingRatesService` port were deleted rather than kept as a pass-through. `EbayControllerImplementation`/`CalculatePricesForLotConsumer` (still legacy) now call `Server.Domain.Shipping.ShippingRatesTable` directly (no DI needed for static reference data) and map to/from the legacy wire contract at their own boundary. Also fixed in the same pass: `ShippingRatesDictionary` no longer rebuilds a `Dictionary` on every access (it's built once, already correctly typed, in the static constructor).
- [x] 2.3 Added `IGeoIpService` port in `Server.Application.Abstractions.Driven` and moved `GeoIpService`/`GeoIpLocation` to new adapter project `Server.Adapters.Driven.GeoIp`, following the existing `Server.Adapters.Driven.ChipFind`/`Server/WellKnown.CacheServiceKeys` pattern for the keyed `IMemoryCache` (adapter owns `CacheServiceKey`, composition root keeps its own duplicated copy of the literal since it can't reference the adapter's internal type). No new unit tests added (the original had none either - it's a thin I/O wrapper); existing `MeasurementPageController` call site updated to the port.
- [x] 2.4 Deleted the now-empty `Server.Application/Services/LotDataExtractor` and `Server.Application/Services/GeoIp` folders; `dotnet build` succeeds solution-wide with no remaining references (verified by grep for both legacy namespaces).

## 3. Measurement/TubeWorkingPoint/MatchedPairDifference: finish moving repositories off `ApplicationDbContext`

`ProductMeasurement`, `TubeWorkingPoint`, `MatchedPairDifference` already have target-shape repository *interfaces*, but `MeasurementRepository`, `TubeWorkingPointsRepository`, `MatchedPairDifferenceRepository` (`Server.Adapters.Driven.EF.WriteModel/Repositories`) all still take `ApplicationDbContext` in their constructor, not `WriteModelDbContext`. This section finishes that move before touching the application-layer services that call them.

- [x] 3.1 Added EF mappings for `ProductMeasurement`, `TubeWorkingPoint`, `MatchedPairDifference` to `WriteModelDbContext` (in the `wm` schema; `ProductMeasurement.LotId` now models its FK to `LotForSale` via EF navigation instead of the raw-SQL cross-context constraint used previously, since both aggregates now live in the same context). Generated migration `MoveMeasurementAggregatesToWriteModel` via `dotnet ef migrations add` creates the three `wm`-schema tables, copies existing rows from the legacy tables, and adds cross-schema FK constraints to the still-legacy `Products` table (`ProductId`/`Id`) guarded by a `conrelid`-scoped `IF NOT EXISTS` check - an unscoped check by `conname` alone is wrong here since Postgres constraint names aren't globally unique and the legacy tables already have same-named constraints (caught by testing against a real seeded Postgres instance, not just a clean-DB dry run). Verified end-to-end against a local Postgres: seeded rows into the legacy tables, ran the migration, confirmed row counts match and FKs land on the `wm.*` tables. `ReadDbContext`'s `ToView` mappings for these three views updated to the `wm` schema (matching the existing `LotForSales`/`MeasurementPhotos` pattern) so reads see the new source of truth. Legacy physical tables are deliberately left in place (not dropped) per `design.md`'s decision - a later, deliberate migration drops them once production cutover is verified.
- [x] 3.2 Repointed `MeasurementRepository`, `TubeWorkingPointsRepository`, `MatchedPairDifferenceRepository` to take `WriteModelDbContext`; dropped the now-redundant mappings/DbSets from `ApplicationDbContext` (plus a now-empty legacy EF migration, `EmptyMigrationAfterMeasurementAggregatesMove`, reconciling the model snapshot - its scaffolded `Up()`/`Down()` were emptied by hand, matching the existing `EmptyMigrationAfterRefactoring` precedent, so the legacy tables aren't dropped by it). **Correction found while verifying, fixed in the same commit:** repointing the repositories alone would have silently broken every caller, since `IUnitOfWork` (bound to `ApplicationDbContext`) was still being injected into `Server.Application.New/MeasurementService.cs` (the already-"migrated" reference implementation for this aggregate) and `Server.Application.New/MatchedPairs/MatchedPairsCalculator.cs` - their `SaveChangesAsync()` calls would no longer touch the context the repositories actually write to. Fixed by switching both to `IWriteModelUnitOfWork` (extended with `BeginTransactionAsync`, mirroring `IUnitOfWork`, since `MeasurementService.DeleteMeasurement` needs a transaction spanning two repositories); `WriteModelDbContext` now implements it the same way `ApplicationDbContext` does. Also fixed `MeasurementPlotWarmupHostedService` (`Server.Application/HostedServices`, still legacy, task 5.4's to move) which read `ApplicationDbContext.ProductMeasurements` directly - the exact risk `design.md` flagged ("route reads through the existing ... query ports"); added `IMeasurementQueries.GetAllMeasurementIds` and switched the hosted service to that port plus `IWriteModelUnitOfWork` for its outbox-flushing save. Verified: `dotnet build` succeeds solution-wide; the full `Tests.Unit` suite (69 tests) passes; `Tests.Integration/Tests/ProductMeasurementFlowTests.cs` (5 tests, exercising the real HTTP pipeline against a freshly-bootstrapped database) passes, including the `DbCommandInterceptor`-based cache test - confirming domain events raised on `ProductMeasurement` still flush to MassTransit through `WriteModelDbContext.SaveChangesAsync` after the move.
- [x] 3.3 **Revised after discovering a real blocker, discussed with the user before proceeding:** `MatchedMeasurementService` never actually depended on `ApplicationDbContext` (its ctor only took `IPublishEndpoint`/`IMeasurementQueries`/`ITubeWorkingPointQueries`/`IMatchedPairDifferenceRepository`/`IUnitOfWork`/`ILogger`) - the task's premise was stale. The real blocker to moving it into `Server.Application.New` was structural: it published ad-hoc `CalculateMatchedPair` commands via MassTransit's `IPublishEndpoint`, which nothing in `Server.Application.New` may depend on directly (that's an adapter-only concern - confirmed by grep: no `Server.Application.New` file references MassTransit, and its `.csproj` has no `MassTransit` package), and it referenced two more legacy-only types (`Server.Application.Consumers.MatchedPairs.CalculateMatchedPair`, `Server.Application.Controllers.NonOkHttpAnswerException`) that a project reference cannot resolve without a cycle. Per the user's direction: instead of inventing a new outbound-publish port, made `ProductMeasurement` raise a real domain event through the machinery `WriteModelDbContext.SaveChangesAsync` already publishes to MassTransit for every tracked aggregate - added `ProductMeasurement.RequestMatchedPairComparison(otherMeasurementId)` (raises new `Server.Domain.Measurements.MatchedPairComparisonRequested`), and rewrote `MatchedMeasurementService` (now `Server.Application.New/MatchedPairs/MatchedMeasurementService.cs`, `public sealed class`, no interface - matching the existing `ProductService`/`LotForSaleService` precedent for services a legacy controller still injects by concrete type) to load each `ProductMeasurement` via `IMeasurementRepository` and call that method instead of publishing directly, batching `IWriteModelUnitOfWork.SaveChangesAsync()` per outer measurement exactly as the original batched `SaveChangesAsync` per outer loop iteration. This obsoletes task 4.5 (see below) rather than deferring it - `CalculateMatchedPair` is deleted, not relocated, and `MatchedPairsCalculatorConsumer` (`Server.Adapters.Driving.MassTransit`) now consumes `MatchedPairComparisonRequested`; its project's now-unneeded `ProjectReference` to `Server.Application` was removed in the same commit. The tube-working-point-not-configured validation error (`NonOkHttpAnswerException.ValidationError400`, HTTP 400) is preserved byte-for-byte: the service now throws the already-referenced `Server.Domain.Exceptions.DomainException` instead, and `EbayControllerImplementation.FindMatchedMeasurementsAsync` (still legacy, same assembly as `NonOkHttpAnswerException`) catches it and re-throws the same `NonOkHttpAnswerException` - mirroring the identical `DomainException` → `NonOkHttpAnswerException` translation `UpsertTubeWorkingPointAsync` in the same file already does for `TubeWorkingPointService`. Verified: `dotnet build` succeeds solution-wide; full `Tests.Unit` (69 tests) and `Tests.Integration` (6 tests, including a new `FindMatchedMeasurements_RequestsComparisonForEachMeasurementPair_AndConsumerComputesTheResult` test added because this exact endpoint had no integration coverage before or after this change) pass; `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` reports the same 67/67 scenarios covered.
- [x] 3.4 **Two blockers found while moving, both resolved with a narrow new port rather than pulling a whole future phase forward:** `MeasurementPlotService` never touched `ApplicationDbContext`/the three repositories either (it's a pure read+render service backed by `IMeasurementQueries`/`ITubeWorkingPointQueries`, already target-shape) - the actual blockers to moving it into `Server.Application.New` were (1) it depended on `DbCache` (`Server.Application/Infrastructure`), which itself wraps `ApplicationDbContext`/`CacheEntry` directly and isn't scheduled to move until task 5.3 - `Server.Application.New` has no project reference back to `Server.Application`, so depending on the concrete class would be circular; (2) it published `MeasurementWatchedOnEbay` via MassTransit's `IPublishEndpoint` directly, the same "adapter concern in the application layer" problem as 3.3, but this one is **not** a fit for the aggregate-domain-event fix used there: the publish is deliberately decoupled from the read path (a plot render is cache-hot; loading and mutating the `ProductMeasurement` aggregate synchronously on every real eBay pageview would add lock contention on the aggregate's optimistic-concurrency row for a side effect the render itself doesn't need) - the existing split into a fire-the-message call here and a separate async consumer (`MeasurementWatchedOnEbayConsumer`/`Handler`, task 3.5) doing the actual aggregate mutation is intentional, not legacy debt. Fixed (1) by adding a narrow `ICacheStore` port (`Server.Application.Abstractions.Driven`) that `DbCache` now implements - the same "interface now, repointed implementation later" shape already used for `IMeasurementRepository` etc. throughout this migration, so task 5.3 can retarget the implementation to `WriteModelDbContext` later without touching this service again. Fixed (2) by adding `IMeasurementWatchedOnEbayPublisher` (same project) implemented by a new `MeasurementWatchedOnEbayPublisher` in `Server.Adapters.Driving.MassTransit` (registered via that project's new `AddMassTransitAdapter()`, called from `Program.cs`) wrapping `IPublishEndpoint.Publish` + `IWriteModelUnitOfWork.SaveChangesAsync`. Also dropped `IMeasurementPlotService` (unused - grepped, nothing outside this module ever referenced it as an interface; both real callers already inject the concrete class, matching `ProductService`/`LotForSaleService`) and added a `ScottPlot` package reference to `Server.Application.New.csproj` (chart rendering; the file's own pre-existing `// todo` about eventually moving rendering to an adapter is left as-is, out of this task's scope). `SvgMerger` moved alongside per 1.2. Verified: `dotnet build` succeeds solution-wide; `Tests.Unit` (69) and `Tests.Integration` (6, including the `DbCommandInterceptor`-based `EbayCurvesAndTubeDescription_DoNotQueryDatabase_OnSecondNotSoldRequest` test and the `EbayCurves_WithoutInternalReferrer_MarksMeasurementAsPublished` test, which exercises the new publisher end to end through the real consumer) pass; coverage checker still reports 67/67.
- [x] 3.5 `MeasurementWatchedOnEbayHandler` already used `IMeasurementRepository`, not `ApplicationDbContext` (same stale-task-premise pattern as 3.3/3.4). Moved it alongside `MeasurementWatchedOnEbayConsumer` into `Server.Adapters.Driving.MassTransit/Consumers/MeasurementWatching`; switched its `IUnitOfWork` to `IWriteModelUnitOfWork` (same fix as every other module in this section - its `SaveChangesAsync()` must hit the context the now-repointed `IMeasurementRepository` actually writes to); registered via the adapter's `AddMassTransitAdapter()` alongside the publisher from 3.4. No dedicated unit test existed for this handler either way; verified with `Tests.Integration`'s `EbayCurves_WithoutInternalReferrer_MarksMeasurementAsPublished`, which exercises this exact handler through the real consumer.
- [x] 3.6 `TubeWorkingPointService` already used `ITubeWorkingPointsRepository` for the aggregate itself, but genuinely did depend on `ApplicationDbContext` directly - for a one-off `Products.AnyAsync(...)` existence check, marked in the source with its own `//todo нужно переделать на запрос в репозиторий` ("should be redone as a repository query"). Fixed by replacing it with `IProductQueries.GetProductAsync(...)` (already exists, backed by the read model, not `ApplicationDbContext`) - resolving that pre-existing TODO as a side effect rather than leaving it next to a newly-"migrated" class. This also removed the dependency on the legacy `NonOkHttpAnswerException.NotFound400()` the not-found branch threw directly: `CreateTubeWorkingPoint` now returns `bool` (`false` = product not found) instead, matching the target-shape convention already used by `Server.Adapters.Driving.WebApi`'s own controllers (null/false result, HTTP shape decided by the caller) rather than inventing a new domain exception for a case `DomainException` doesn't fit (that type's catch block in `EbayControllerImplementation` already means something more specific - the working point's own invariant failure - and collapsing both into it would require distinguishing them some other way). `EbayControllerImplementation.UpsertTubeWorkingPointAsync` (still legacy) now throws `NonOkHttpAnswerException.NotFound400()` itself when the result is `false`, preserving the exact prior response for that case; the existing `DomainException` → `ValidationError400` catch for the invariant-failure case is unchanged and still runs first, matching the original check order (product existence before working-point validation). Moved into `Server.Application.New/TubeWorkingPoints` (plural, to avoid a namespace/type-name collision with `Server.Domain.Measurements.TubeWorkingPoint`). Verified: `dotnet build` succeeds; `Tests.Unit` (69, including `TubeWorkingPointTests`, unaffected since the aggregate's own validation wasn't touched) and `Tests.Integration` (6) pass; coverage checker still 67/67 (the `tube-working-point` capability's scenarios cover only the aggregate's own validation, which this task didn't change).
- [x] 3.7 Already done as a consequence of 3.3-3.6 - each task deleted its own file(s) as part of the move (`git mv`/`git rm`, not a separate copy-then-delete step), so `Server.Application/Services` is already gone entirely (confirmed: the directory no longer exists). Verified `dotnet build` succeeds solution-wide and `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` reports 67/67 scenarios covered (see 3.6's note).

## 4. PriceCalculator, EbayCurvesCacheWarmUp, and the leftover MatchedPairs consumer

`CalculatePricesForLotConsumer` reads `Currencies`, `Lots` (with a `Purchases` include), and `Products` directly from `ApplicationDbContext`. `Product` has a repository interface but its implementation (`ProductRepository`) still takes `ApplicationDbContext`. `Lot` (an eBay listing: price/shipping/condition/currency/`Purchases`) is unrelated to the already-migrated `LotForSale` aggregate and has no domain design yet. This phase must do real domain design work, not just redirect call sites.

- [x] 4.1 **Scoped to the `Product`-owned slice of the phase's four named messages/consumers, per the section split with 4.2 below:** of `CalculateMetricsForProduct`/`CalculatePricesForAll`/`CalculatePricesForLot`/`CalculatePricesForProductConsumer`, only `CalculateMetricsForProductConsumer`'s aggregation (`ProductCalculationResult` from `LotCalculationResult`s) is `Product`-aggregate domain logic; `CalculatePricesForLot`'s per-purchase/shipping math belongs to the not-yet-designed `Lot`/`Purchase` aggregates (task 4.2), so it is untouched here - moving it now would mean guessing `Lot`'s shape before its own design step. Added `Product.RecalculateMetrics(lotCalculationResults, unpublishedOnEbayCountCreated, unpublishedOnEbayCountSelling)` (mirrors the legacy inline loop exactly - same running sums, same "earliest calculation date, defaulting to now for zero lots" rule) and made `ProductCalculationResult`'s setter `private`; `CalculateMetricsForProductConsumer` (still legacy, not moved to `Server.Application.New` - that's task 4.3) now calls it instead of assigning the property directly. **Blockers found while moving `Product` to `WriteModelDbContext`, resolved narrowly rather than pulled into 4.2:** (1) `Lot`/`IgnoredLot`/`ProductPassport`/`ProductEmailSendHistory` all carry a cross-aggregate `Product` navigation (plus a stray, entirely unused `DbSet<Product> Products` on `ApplicationUser`) that pulled `Product` into `ApplicationDbContext`'s model by convention even after its own `DbSet<Product>`/entity config were removed; fixed with a single `builder.Ignore<Product>()` (simpler than four per-navigation `Ignore` calls) and by deleting the dead `ApplicationUser.Products` property - grepped, none of these navigations had a real caller. (2) **Correction after PR review (`chatgpt-codex-connector`):** the legacy `Lots`/`IgnoredLots`/`ProductPassports`/`SaleAdvertisements` tables' FKs to the legacy `Products` table would incorrectly reject rows for any product created after cutover (new products only land in `wm.Products` now); a first pass just dropped these four FKs via a new legacy EF migration (`DropLegacyProductForeignKeysAfterProductMove`, hand-edited after `dotnet ef migrations add` auto-generated - as expected once `Product` left the model - `DropTable`s for `Products`/`Product_SearchQueries`/`Product_RuSearchQueries` and a `DropIndex` on `Lots.ProductId`; removed those four following the `EmptyMigrationAfterMeasurementAggregatesMove` precedent, keeping the legacy tables and the still-hot `ProductId` index), but review correctly flagged that leaving these four references unconstrained drops referential integrity for every legacy child table (e.g. `UpsertLotInfoAsync` would then accept a nonexistent `productId` with no error). Fixed by re-adding all four, pointing at `wm."Products"`, from the *write-model* migration instead of the legacy one - Program.cs migrates `ApplicationDbContext` before `WriteModelDbContext`, so `wm.Products` doesn't exist yet when the legacy migration runs, and the legacy migration already dropped the old FKs by the time the write-model migration runs second - `ON DELETE RESTRICT` (not the original `CASCADE`) per `AGENTS.md`'s "cross-aggregate FKs use RESTRICT, not CASCADE" rule, since `Lot`/`IgnoredLot`/`ProductPassport`/`ProductEmailSendHistory` and `Product` are different aggregates now that `Product` has its own schema. (3) `wm.ProductMeasurements`/`wm.TubeWorkingPoints` already had FKs to the legacy `Products` table (added in `MoveMeasurementAggregatesToWriteModel` anticipating this exact move); repointed both to `wm.Products` via raw SQL in the new write-model migration, after the data copy so existing rows validate against it. (4) `ProductRepository`'s `TransactionHelper.EnsureRepeatableReadOrStartAsync` was typed to `ApplicationDbContext`; widened to the base `DbContext` (the method only ever used `.Database`) instead of adding an overload. (5) `EbayControllerImplementation`'s two direct `_applicationContext.Products.AnyAsync(...)` existence checks and `CalculatePricesForLotConsumer`'s direct `_applicationContext.Products...` read (for `product.Weight`) would have silently gone stale post-cutover; repointed to `_productService.GetProductAsync`/`IProductQueries.GetProductAsync` respectively (same fix shape as task 3.6) - the latter read was also reordered to happen *before* `CalculatePricesForLotConsumer` opens its ambient `TransactionScopeFactory` scope, since `IProductQueries` opens its own transaction on a second connection (`ReadDbContext`) and the configured connection string has `Enlist=true`, so a second enlisting connection inside the same ambient scope would require a distributed transaction Npgsql doesn't support. Added `Server.Domain`/`Server.Application.Abstractions.Driven` as `Server.Adapters.Driven.EF.WriteModel`'s own `ProjectReference`s (previously only transitive through the now-removed `Server.Application`/`Server.Application.New` references) plus the `MassTransit.EntityFrameworkCore`/`Npgsql.EntityFrameworkCore.PostgreSQL`/`Microsoft.Extensions.Configuration`/`Microsoft.Extensions.DependencyInjection.Abstractions` packages it turned out to need directly (same reason); `Server.Adapters.Driven.EF.WriteModel.Migrations`' design-time factory needed `Microsoft.Extensions.Configuration.{Json,FileExtensions}` for the same reason (new `PackageVersion`s added to `Directory.Packages.props`). `ReadDbContext`'s `Products`/`Product_SearchQueries`/`Product_RuSearchQueries` `ToView`/`ToTable` mappings repointed to the `wm` schema (matching the existing `LotForSales`/`MeasurementPhotos` pattern). Verified: `dotnet build` succeeds solution-wide; new `Tests.Unit/ProductTests.cs` (3 tests) pins `RecalculateMetrics`'s sums/earliest-date/zero-lots behavior as a byte-identical characterization of the removed inline loop; full `Tests.Unit` (72) and `Tests.Integration` (30, only the 2 pre-existing Playwright-environment failures unrelated to this change - `chrome-headless-shell` isn't provisioned in this sandbox) pass against a freshly-bootstrapped database, including `TestHelpers.CreateProductAsync`'s existing create/read roundtrip through the new `wm.Products` table; `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` reports 70/70. **Not added:** an end-to-end integration test publishing `CalculateMetricsForProduct` directly and asserting the consumer's effect - MassTransit's bus outbox (`AddEntityFrameworkOutbox<WriteModelDbContext>().UseBusOutbox()`) did not deliver a message published this way (confirmed stuck in `wm."OutboxMessage"`) when driven directly from test code outside an HTTP request/consume pipeline, a pre-existing infrastructure quirk unrelated to this task's change (the consumer's registration/batching wasn't touched); the domain math itself is covered by the unit tests above instead.
- [x] 4.2 `Lot` became its own `AggregateRoot<long>` in `Server.Domain` (same namespace/file it already lived in, not a `Server.Domain.Lot.Lot` sub-namespace like `Product` - that would collide with the existing `Server.Domain.Lot` legacy type name during the transition) - chosen over folding into `Product` because Lots are upserted far more often than Product itself changes (every eBay scrape), and coupling them into one aggregate would create optimistic-concurrency contention on the Product row for writes that don't need to be transactionally consistent with it (matches the existing precedent: `ProductMeasurement`/`TubeWorkingPoint` are separate aggregates referencing `Product` by `ProductId` only, not folded in either). `Purchase` is an owned child of `Lot` (not its own aggregate - it has no independent lifecycle or identity outside a specific Lot's purchase history) with a private constructor/`Create` factory and `UpdateAmount`/`SetCalculationResult` domain methods; kept its natural composite key (`LotId`+`Date`, matching the legacy schema) rather than inventing a synthetic `Guid Id` it has no real use for. `Currency` also became its own small `AggregateRoot<string>` (keyed by `CurrencyEbayName`) with `Create`/`UpdateRate`, for consistency with every other aggregate in `WriteModelDbContext` (the `IAggregateRoot`-based row-version loop, audit columns) even though it never raises domain events. All three moved to `Server.Adapters.Driven.EF.WriteModel`/`wm` schema in one migration (`MoveLotPurchaseCurrencyToWriteModel`) since `Lot` has real FKs to both `Product` and `Currency` that only resolve if all three land together; `Lot.Categories` (hstore) needed a `HasConversion` (`IReadOnlyDictionary<string,string>` isn't itself a supported Npgsql hstore CLR type) with an explicit `ValueComparer` (EF warned it couldn't detect in-place dictionary mutations otherwise) - `Lot.UpdateInfo` replaces the whole `_categories` dictionary object rather than mutating it in place, matching what the comparer expects. Data copy in the same migration (Currencies before Lots before Lot_Purchases, respecting the FK order); `CreatedAt`/`ChangedAt` (required by `IAuditable`, absent from the legacy `Lots`/`Currencies` tables) backfilled from `UpdateDate`/`LastUpdate` respectively rather than `now()`, to keep a meaningful value instead of a migration-time artifact. Companion legacy migration (`DropLegacyLotPurchaseCurrencyAfterMove`) is a full no-op, unlike the equivalent step for `Product` in 4.1 - `Lots`/`Purchases`/`Currencies` are the tables being retired here (nothing else still written through `ApplicationDbContext` references them, unlike `Products`, which `IgnoredLots`/`ProductPassports`/`SaleAdvertisements` still referenced), so there is no stale FK to drop; kept the legacy tables' own FKs and the `hstore` extension in place too, since dropping either is inert now that nothing writes to these frozen tables, and `WriteModelDbContext`'s own migration already re-declares the `hstore` extension for `wm.Lots.Categories` regardless. **Consumer/controller call sites forced to change immediately, same shape as 4.1's blockers:** `EbayControllerImplementation`'s Lot/Purchase/Currency methods (`GetLotsAsync`, `UpsertLotInfoAsync`, `IgnoreLotsAsync`, `GetLotInfoAsync`, `DeleteLotInfoAsync`, `GetLotIdsAsync`, `GetLotStatesAsync`, `GetCurrenciesAsync`) moved off `ApplicationDbContext` onto new `ILotRepository`/`ILotQueries`/`ICurrencyQueries`/`IWriteModelUnitOfWork` ports (new `LotDetails`/`PurchaseDetails`/`LotUpdateInfo`/`CurrencyInfo` query DTOs, `LotQueries`/`CurrencyQueries` read-model implementations backed by an extended `LotView`/new `PurchaseView`/`CurrencyView`) - per `AGENTS.md`'s command/query split, reads go through the query ports, not the repository. `UpsertLotInfoAsync`'s bulk `FlexLabs.EntityFrameworkCore.Upsert` calls (which only ever worked against a mutable, publicly-settable entity) were replaced with a proper load-or-create-then-domain-method-then-save flow through `ILotRepository`; `ModelsExtensions.ToDbLot`/`ToDbPurchase` (object-initializer construction, incompatible with the aggregates' private setters) were deleted, `ToApiLot`/`ToApiLotInfoShort`/`ToApiPurchaseInfo`/`ToApiCurrency` repointed to the new query DTOs. `CalculatePricesForLotConsumer`/`CalculatePricesForProductConsumer` (still legacy, not moved to `Server.Application.New` - task 4.3) repointed the same way; `CalculatePricesForLotConsumer`'s now-single-context flow (`ILotRepository`+`IWriteModelUnitOfWork`) no longer needs `TransactionScopeFactory` at all (its previous job - keeping the Currency/Lot/Purchase reads and the save atomic across one `ApplicationDbContext` connection - is now moot: reads go through the read model, the one write goes through one `WriteModelDbContext` unit of work), so it was dropped from that consumer entirely; this happens to satisfy part of what task 4.3 listed as its own follow-up, as a direct consequence rather than pulled-forward scope. `CurrencyRateBackgroundTask` (task 5.2's to move) hit the same wall (`.Attach(new Currency{...})` + direct property mutation) and got the same narrow in-place fix (`ICurrencyRepository`+`Currency.UpdateRate(...)`) without moving the hosted service itself. `IgnoreLotsAsync`'s existence check and `CalculatePricesForLotConsumer`'s product/currency reads are ordered before opening any `TransactionScopeFactory` scope, same `Enlist=true`-distributed-transaction reasoning as 4.1's equivalent fix. Verified: `dotnet build` succeeds solution-wide; new `Tests.Unit/LotTests.cs` (5 tests) and `Tests.Unit/CurrencyTests.cs` (2 tests) cover the extracted domain methods; full `Tests.Unit` (79) and `Tests.Integration` (31, same 2 pre-existing Playwright-environment failures as 4.1, unrelated) pass against a freshly-bootstrapped database; new `Tests.Integration/Tests/LotPricingFlowTests.cs` exercises `UpsertLotInfoAsync` end-to-end through the real HTTP endpoint and polls for `CalculatePricesForLotConsumer` to compute and persist `LotCalculationResult`/`PurchaseCalculationResult` through the new aggregate-backed storage (this one wasn't blocked by 4.1's bus-outbox quirk, since it publishes from within a real HTTP request scope rather than raw test code); `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` reports 70/70.
- [x] 4.3 Added `ILotPriceCalculator`/`IProductMetricsCalculator` (`Server.Application.Abstractions.Driving.Abstractions.Services`) with `LotPriceCalculator`/`ProductMetricsCalculator` implementations in `Server.Application.New/PriceCalculator` - the two scenarios with real business logic (the elaborate shipping/fee/currency math, and the lot-results-to-product-metrics aggregation, both moved verbatim from the legacy consumers, already extracted onto domain methods in 4.1/4.2). `CalculatePricesForAll`/`CalculatePricesForProductRequested`'s handling is pure fan-out (read ids, publish one message per id, zero business logic) so it stayed a thin driving-adapter consumer calling `IProductQueries`/`ILotQueries` directly rather than adding a pass-through service with nothing in it - not every consumer needs an application-layer service, only the ones with actual orchestration/business rules. `IPublishEndpoint` can't be a dependency of `Server.Application.New` (MassTransit is an adapter concern, per `AGENTS.md`'s hexagonal rule and the existing `MatchedPairsCalculatorConsumer` precedent) so both calculators return data instead of publishing themselves - `LotPriceCalculator.CalculateAsync` returns the lot's `ProductId`, and the new `Server.Adapters.Driving.MassTransit/Consumers/PriceCalculator/CalculatePricesForLotConsumer` publishes `CalculateMetricsForProduct` with it after the calculator's own `IWriteModelUnitOfWork.SaveChangesAsync` completes (previously this publish was queued as part of the same `ApplicationDbContext.SaveChangesAsync` call; now that the aggregate write and the message publish are on the write-model unit of work but two separate steps, this relies on MassTransit's consumer-scoped `ConsumeContext.Publish` reliability rather than being bundled into one outbox-writing `SaveChanges` call - verified this still works via `LotPricingFlowTests`, run through the real HTTP → consumer chain, not raw publish from test code). Moved the four message contracts (`CalculatePricesForLot`, `CalculateMetricsForProduct`, `CalculatePricesForProductRequested`, `CalculatePricesForAll`) to `Server.Application.Abstractions.Driving.Abstractions.Messages`, matching the existing `MeasurementWatchedOnEbay` precedent for a non-domain-event application message; added the four matching consumers to `Server.Adapters.Driving.MassTransit/Consumers/PriceCalculator` and deleted the legacy `Server.Application/Consumers/PriceCalculator` folder outright (not left alongside the new ones). `TransactionScopeFactory` in `CalculatePricesForLotConsumer` was already removed as a direct consequence of task 4.2's move to a single `WriteModelDbContext` unit of work (documented there) - nothing left to remove here. **Bug fix that fell out of the extraction:** the legacy `CalculateMetricsForProductConsumer`'s batch loop did `return` (not `continue`) when a product in the batch wasn't found, silently abandoning every other product in that batch; since the per-product logic is now its own method (`ProductMetricsCalculator.CalculateAsync`) called once per id from the consumer's loop, a missing product now only skips that one id - not a deliberate task 4.3 goal, but a correctness fix the refactor made unavoidable to introduce (returning early inside the method can no longer reach past iterations it was never part of). Moved the `WellKnown.Ebay` pricing constants (and reused the already-existing, numerically identical `WellKnown.EbayWeightMultiplier` instead of duplicating a `множительДляУчетаВесаУпаковки` alongside it) from `Server.Application.WellKnown` to `Server.Application.New.WellKnown`, since the calculator using them now lives there and can't reference legacy `Server.Application`. Verified: `dotnet build` succeeds solution-wide; new `Tests.Unit/LotPriceCalculatorTests.cs` and `Tests.Unit/ProductMetricsCalculatorTests.cs` (3 tests) cover both calculators against hand-written port fakes (matching this project's existing no-mocking-library convention); full `Tests.Unit` (82) and `Tests.Integration` (31, same 2 pre-existing Playwright-environment failures as 4.1/4.2, unrelated) pass, including `LotPricingFlowTests` (added in 4.2) re-verified end-to-end against the new consumers/services - proving a calculation request produces a persisted result through the fully moved pipeline; `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` reports 70/70.
- [x] 4.4 **No new use-case service needed:** the consumer already delegated entirely to `MeasurementPlotService` (already in `Server.Application.New` since task 3.4) - moving it was a mechanical relocation, not a design step. Moved the message record to `Server.Application.Abstractions.Driving.Abstractions.Messages` (same as 4.3's messages) and the consumer itself to `Server.Adapters.Driving.MassTransit/Consumers/EbayCurvesCacheWarmUp`, injecting `MeasurementPlotService` by concrete type - matching the established precedent for this specific service (task 3.4 deliberately dropped its unused `IMeasurementPlotService` interface, and `Server.Adapters.Driving.WebApi` already references `Server.Application.New` concrete services directly the same way) rather than resurrecting an interface nothing else needs. Added `Server.Adapters.Driving.MassTransit`'s first `ProjectReference` to `Server.Application.New` for this. The consumer's `catch (DbUpdateException)` around the cache warm-up (absorbing a benign write race from `ICacheStore`'s still-legacy `DbCache` implementation) had to move with it verbatim to preserve behavior, which forces an EF Core package reference onto this driving-adapter project that doesn't otherwise belong there - flagged with a `// TODO` (per the newly-agreed convention: leave deferred fixes as TODO comments, not just chat/review notes) pointing at task 5.3's `DbCache` move as the natural place to absorb this at its actual source instead. Verified: `dotnet build` succeeds solution-wide; full `Tests.Unit` (82) and `Tests.Integration` (31, same 2 pre-existing Playwright-environment failures, unrelated) pass, including the existing cache-warmup-dependent assertions in `ProductMeasurementFlowTests.cs` (`EbayCurvesAndTubeDescription_DoNotQueryDatabase_OnSecondNotSoldRequest` and friends), which exercise the same `MeasurementPlotService`/`ICacheStore` path this consumer warms.
- [x] 4.5 **Done early, as part of task 3.3, by replacing rather than relocating:** moving `MatchedMeasurementService` (3.3) required resolving its `CalculateMatchedPair` dependency then and there, since it's what stood between it and `Server.Application.New` - see 3.3's note for the full reasoning. `CalculateMatchedPair` is deleted (not moved); `MatchedPairsCalculatorConsumer` consumes the new `Server.Domain.Measurements.MatchedPairComparisonRequested` domain event instead; `Server.Application/Consumers/MatchedPairs/` is already gone (only `PriceCalculator`/`EbayCurvesCacheWarmUp` remain under `Consumers/`, this phase's own scope); `Server.Adapters.Driving.MassTransit`'s `ProjectReference` to `Server.Application` - the exact hidden legacy reference `design.md`'s Risks section flagged - is removed. Verified no remaining reference to the old message type (grep) and with the `Tests.Integration` suite (see 3.3).
- [x] 4.6 Already empty after 4.3/4.4 each deleted their own legacy folder (`PriceCalculator`, `EbayCurvesCacheWarmUp`) as part of their own move - removed the now-empty `Server.Application/Consumers` directory itself. Verified: grepped for any remaining `Server.Application.Consumers` reference solution-wide (none); `dotnet build` succeeds solution-wide; `Program.cs`'s `AddMassTransit` registers only consumers from `Server.Adapters.Driving.MassTransit.Consumers.*`.

## 5. Hosted services

- [x] 5.1 Moved `ChipfindBackgroundTask`/`IChipfindAdapter`/`IEmailSender`/`SaleAdvertisement` into `Server.Application.New` (`HostedServices/ChipFind`), with port interfaces (`IChipfindAdapter`, `IEmailSender`) and the `SaleAdvertisement` record moved into `Server.Application.Abstractions.Driven` (`Abstractions/`, `Models/`), still implemented by `Server.Adapters.Driven.ChipFind`/`Server.Adapters.Driven.Smtp` (repointed to the new port namespace; both needed extra `PackageReference`s - `Microsoft.Extensions.Caching.Memory`/`.Http`/`.DependencyInjection.Abstractions`/`.Logging.Abstractions` for ChipFind, `Microsoft.Extensions.Options`/`.ConfigurationExtensions`/`.DataAnnotations` for Smtp - since they'd been getting these transitively through `Server.Application`'s ASP.NET Core shared-framework reference; both new `PackageVersion`s added to `Directory.Packages.props`). Removed `TransactionScopeFactory` from `ChipfindBackgroundTask` entirely rather than leaving it partially legacy: its only DB touch was `ApplicationDbContext.ProductEmailSendHistory` (find-or-create dedup writes), so `ProductEmailSendHistory` itself was pulled forward from task 6.3's batch (it was bundled there with `IgnoredLot`/`ClientError`) and moved to `WriteModelDbContext` now, as its own aggregate (`AggregateRoot<Guid>`, `Create`/`UpdateForNewAdvertisement`, client-generated `Guid.NewGuid()` id matching every other surrogate-key aggregate in this codebase - `ValueGeneratedNever()` is the established convention here, unlike the legacy int-identity column) with `IProductEmailSendHistoryRepository`/`IProductEmailSendHistoryQueries`/`ProductEmailSendHistoryDetails` ports, a `ProductEmailSendHistoryView` read-model projection, and a migration (`MoveProductEmailSendHistoryToWriteModel`) copying data into `wm."ProductEmailSendHistories"` with a `Guid` FK to `wm."Products"` (`ON DELETE RESTRICT`) - paired with an emptied `DropLegacyProductEmailSendHistoryAfterMove` legacy migration that leaves the physical `"SaleAdvertisements"` table and its FK in place, mirroring `DropLegacyLotPurchaseCurrencyAfterMove`. The legacy entity's own `CreatedAt` field (the advertisement's publish date, not an audit timestamp) had to be renamed to `AdvertisementDate` on the new aggregate, since `AggregateRoot`/`Entity` already reserve `CreatedAt`/`ChangedAt` as EF-managed audit fields that `WriteModelDbContext.ApplyAudit()` overwrites on save - the legacy `ApplicationDbContext` never ran this interceptor over the plain (non-`Entity<TId>`) legacy type, so the collision only surfaces now. `EbayControllerImplementation.GetSaleAdvertisementsAsync` (still-legacy, phase 6.3) was repointed from `_applicationContext.ProductEmailSendHistory` to the new `IProductEmailSendHistoryQueries` port rather than left on the dead DbSet. `ChipfindBackgroundTask`'s per-advertisement find-or-create loop replaced the original `.Local.FirstOrDefault(...) ?? await FirstOrDefaultAsync(...)` (which coalesced duplicate matches against the same product within one advertisement via the change tracker) with an explicit `Dictionary<Guid, ProductEmailSendHistory>` cache keyed by product id, since the read-model query port is a separate connection that can't see not-yet-saved writes on `WriteModelDbContext` - without this the second match for the same product within one advertisement would attempt a duplicate insert and violate the new unique index. `EbayServerOptions` and the `WellKnown.ChipFind`/`WellKnown.SaleAdvertisements` constants were also pulled forward into `Server.Application.New` (needed by the hosted service's new home; the `EbayServerOptions` binding/registration moved with it, since the 3 other still-legacy consumers - `CurrencyRateBackgroundTask`, `MeasurementPlotWarmupHostedService`, `DbCache` - already referenced `Server.Application.New` and could just add a `using`). Registered from `Server.Application.New/ServiceCollectionExtensions.cs` (which `Server.Application`'s `AddApplicationServices()` already calls transitively, same as every other already-migrated `Server.Application.New` service - no direct `Program.cs` call needed, consistent with existing precedent). Added `ProductEmailSendHistoryTests` (domain unit tests for `Create`/`UpdateForNewAdvertisement`, including the "blank contact doesn't overwrite" rule). Verified: full solution builds, `Tests.Unit` (82/82) and `Tests.Integration` (29/31, the 2 pre-existing Playwright-environment failures) pass, `chipfind-monitoring`'s existing `ChipfindAdapterTests` `[OpenSpecScenario]` mappings still resolve (only their `using` changed), `openspec validate --all --strict` passes, and `check-openspec-test-coverage` reports 70/70 scenarios covered.
- [x] 5.2 Moved `CurrencyRateBackgroundTask` into `Server.Application.New/HostedServices/Currencies`, registered from the same `ServiceCollectionExtensions.cs` as 5.1/5.5. The direct `OpenExchangeRatesClient` (`OpenExchangeRates.NET` package) call was wrapped behind a new `ICurrencyRateSource` port (`Server.Application.Abstractions.Driven`) implemented by a new driven adapter project, `Server.Adapters.Driven.OpenExchangeRates` (added to `Ebay.slnx`, referenced from `Server.csproj`, registered via its own `AddOpenExchangeRatesAdapter()` from `Program.cs`) - the port returns a plain `IReadOnlyDictionary<string, double>` of rates by API currency name, keeping the `OpenExchangeRates.NET` package's `RatesResponse`/`decimal` types (confirmed via reflection against the installed package, since no local source is vendored) entirely inside the adapter, same as `IChipfindAdapter` returning plain `SaleAdvertisement` records instead of raw XML. `WellKnown.CurrencyRate.UpdateTime`/`ErrorDelay`/`BaseCurrency` moved to `Server.Application.New.WellKnown` alongside the hosted service; `AppId` (a literal API-key constant, not bound to configuration anywhere in this repo - no `appsettings.json` exists in source control and no deploy config references it, so it's genuinely the production value today, not a placeholder) moved into the new adapter as an adapter-internal constant instead, since it's an implementation detail of the OpenExchangeRates client, not an application-layer constant; left as a hardcoded literal rather than switched to `IOptions` binding, since that would be a deployment-configuration change outside this task's architectural scope and unverifiable from this sandbox (no way to confirm a corresponding env var exists in production). Removed the now-unused `OpenExchangeRates.NET` `PackageReference` from `Server.Application.csproj`. No pre-existing "currency-rate tests" were found (no openspec capability, no test file references `CurrencyRateBackgroundTask`/`OpenExchangeRates`) - verified instead via full solution build, `Tests.Unit` (85/85) and `Tests.Integration` (31/31, including the DI-graph validation `Server`'s `UseDefaultServiceProvider(ValidateOnBuild/ValidateScopes = true)` performs on every host build/test run), `openspec validate --all --strict`, and `check-openspec-test-coverage`.
- [x] 5.3 Moved `CacheEntry` into `WriteModelDbContext` (`wm."CacheEntries"`, via a new `MoveCacheEntryToWriteModel` migration copying existing rows - `design.md` calls `CacheEntry` a "mechanical copy" table with no owning behavior, so data was still carried over for consistency with every other legacy-to-wm move even though a stale/expired row here is harmless, unlike `Product`/`Lot`; paired with an emptied `DropLegacyCacheEntryAfterMove` legacy migration, same shape as 5.1/4.2's). `DbCache` itself - since it directly needs a `DbContext`, unlike a pure `Server.Application.New` use-case service - moved into `Server.Adapters.Driven.EF.WriteModel` (not `Server.Application.New`, despite the task wording's "wherever DbCache itself ends up" phrasing implying otherwise: an adapter class can't live in the application layer, the dependency only runs the other way) as the `ICacheStore` implementation, alongside the other `WriteModelDbContext`-backed repositories; `CacheEntry` itself became an `internal` adapter-private persistence type there too (it was never domain data - a plain mutable cache row, not a `Server.Domain` aggregate), so nothing outside this one adapter project needs to know its shape. `DatabaseConcurrentAccessSemaphore` (only ever consumed by `DbCache`) moved alongside it for the same reason, registered as the same connection-pool-sized singleton it always was. `DbCacheCleanupHostedService` moved into `Server.Application.New/HostedServices/DbCache`, registered from the same `ServiceCollectionExtensions.cs` as 5.1/5.2/5.5 - repointed from resolving the concrete `DbCache` class (which the hosted service could no longer reference, being in a different layer) to the `ICacheStore` port instead, which required promoting `RemoveOldVersionsAsync` from `DbCache`-only onto the `ICacheStore` interface itself, since cache-maintenance is a reasonable capability for any cache-store implementation to expose. **Resolved the task-4.4 TODO in the same commit, as intended by that comment:** `CalculateEbayCurvesForMeasurementConsumer`'s `catch (DbUpdateException)` (and the `Microsoft.EntityFrameworkCore` package reference on `Server.Adapters.Driving.MassTransit.csproj` it forced) is gone - `DbCache.GetOrCreateAsync` now catches and logs that exception itself around its own `SaveChangesAsync`, the same message the consumer used to log, so the EF-specific detail no longer leaks across the driving-adapter boundary. `EbayServerOptions` (needed by `DbCache.IsLocalRun` check) pulled a new `Server.Application.New` `ProjectReference` into `Server.Adapters.Driven.EF.WriteModel.csproj` - matches the existing precedent of `Server.Adapters.Driving.MassTransit` already depending directly on a concrete `Server.Application.New` type (`MeasurementPlotService`) for the same kind of reason. `WellKnown.DbCache.Version`/`WellKnown.ImageCache` split: `Version` (an adapter-internal cache-versioning detail, not an application-layer constant) became a private `const` directly in `DbCache.cs` rather than a new cross-project `WellKnown` dependency. Verified: full solution builds; `Tests.Unit` (85/85) and `Tests.Integration` (31/31) pass against a freshly-bootstrapped, freshly-migrated database (confirming the full DI graph - `ICacheStore`→`DbCache`→`WriteModelDbContext`/`DatabaseConcurrentAccessSemaphore`/`EbayServerOptions` - resolves under `Server`'s `ValidateOnBuild/ValidateScopes` host validation); `openspec validate --all --strict` passes; `check-openspec-test-coverage` reports 70/70. No pre-existing "cache-cleanup tests" were found (same gap noted for 5.2's currency-rate tests) and no integration test currently exercises the HTTP path that would trigger `MeasurementPlotService`'s cache reads/writes (a pre-existing gap, not introduced here, and outside any openspec scenario) - relied on the identical migration/build/DI verification used throughout this phase instead.
- [x] 5.4 Moved `MeasurementPlotWarmupHostedService` into `Server.Application.New/HostedServices/Measurements`, registered from the same `ServiceCollectionExtensions.cs` as the rest of phase 5; moved `EnumerableExtensions.Batch` alongside it into `Server.Application.New/MeasurementPlot` (matching where `MeasurementPlotService` itself already lived from task 3.4, since `Batch` has no other caller). The only wrinkle: this is the first `Server.Application.New` class to call `IPublishEndpoint.Publish` directly (every other `Server.Application.New` service either doesn't publish, or raises a domain event that `WriteModelDbContext.SaveChangesAsync` publishes on its behalf) - required a new `PackageReference Include="MassTransit"` on `Server.Application.New.csproj`. Treated as the kind of unavoidable exception the project's own comment there allows ("external dependencies only when the layer can't be implemented without them") rather than inventing a new publish-abstraction port with a single caller, since no such abstraction exists anywhere else in this codebase to reuse and the hosted service's entire job is exactly this fan-out publish. `Server.Application/HostedServices` is now completely empty (task 5.7's to delete). Verified: full solution builds; `Tests.Unit` (85/85) and `Tests.Integration` (31/31) pass. No pre-existing "warmup tests" were found (same gap as 5.2/5.3's currency-rate/cache-cleanup tests - no test references `MeasurementPlotWarmupHostedService`) - relied on the same build/DI-validation/migration verification used throughout this phase.
- [x] 5.5 Removed the `TransactionScopeFactory` usage in `SaleAdvertisementCleanupBackgroundTask` (it now deletes stale rows via `IProductEmailSendHistoryRepository.RemoveOlderThanAsync`, an `ExecuteDeleteAsync`-based bulk delete against `WriteModelDbContext` alone, matching the existing `MeasurementRepository`/`LotRepository`/`ProductRepository` bulk-delete convention - no `SaveChangesAsync` needed since `ExecuteDeleteAsync` executes immediately); moved it alongside the ChipFind module from task 5.1 into `Server.Application.New/HostedServices/SaleAdvertisements`, registered from the same `ServiceCollectionExtensions.cs`. Done together with 5.1 in the same commit since both hosted services shared the `ProductEmailSendHistory` move and the `BackgroundTask.cs` relocation below - splitting them would have left one half of the pair depending on a not-yet-existing new home. Verified with the same build/test/openspec run as 5.1 (no dedicated cleanup-specific tests existed before or after; the deletion logic is a straightforward `ExecuteDeleteAsync` with no branching to unit-test beyond what the repository layer already covers by convention).
- [x] 5.6 **Sequencing correction (partially pulled forward, same reasoning as 1.1/1.2):** moving `ChipfindBackgroundTask` and `SaleAdvertisementCleanupBackgroundTask` in 5.1/5.5 left only one remaining derived class (`CurrencyRateBackgroundTask`, still in legacy `Server.Application`, phase 5.2) - i.e. the "majority" this task waits for was already reached. Moved `Infrastructure/BackgroundTask.cs` and `ExceptionExtensions.IsNotIntendedCancellation` (from `Server.Application/Infrastructure`, see 1.1) into `Server.Application.New/Infrastructure` now rather than leave 5.1/5.5's two hosted services duplicating the base class or reaching backwards into legacy `Server.Application` for it (the latter would have been a real architecture violation, not just a temporary wart). `CurrencyRateBackgroundTask` (still physically in `Server.Application`) only needed its `using Server.Application.Infrastructure;` repointed to `using Server.Application.New.Infrastructure;` - a one-line change, not a full move, since `Server.Application` already depends on `Server.Application.New`. Nothing left for this task when 5.2 moves `CurrencyRateBackgroundTask` itself; that task should just confirm the base class is already in place. Verified: solution builds, all hosted services (including the still-legacy `CurrencyRateBackgroundTask`, `DbCacheCleanupHostedService`, `MeasurementPlotWarmupHostedService`) still resolve their DI graph (`Server`'s own build + `UseDefaultServiceProvider(ValidateOnBuild/ValidateScopes = true)` in `Program.cs` would fail the build/startup otherwise).
- [x] 5.7 `Server.Application/HostedServices` was already empty after 5.1-5.5 moved every hosted service out of it (`DbCacheCleanupHostedService`/`MeasurementPlotWarmupHostedService` in 5.3/5.4 were the last two); removed the now-empty directory. All 6 hosted service registrations (`ChipfindBackgroundTask`, `SaleAdvertisementCleanupBackgroundTask`, `CurrencyRateBackgroundTask`, `DbCacheCleanupHostedService`, `MeasurementPlotWarmupHostedService`, `MeasurementPhotoOriginalSizeBackfillHostedService`) now live in `Server.Application.New/ServiceCollectionExtensions.cs`'s `AddApplicationNewServices()` (called transitively from `Server.Application`'s `AddApplicationServices()`, itself called from `Program.cs` - no direct per-service `Program.cs` registration was ever needed, consistent with every other `Server.Application.New` service registered this way throughout phase 4-5); confirmed via `grep -rn "AddHostedService<"` that no registration references a since-deleted legacy type. `Server.Application`'s own `ServiceCollectionExtensions.cs` no longer has a single `AddHostedService` call. Verified: `dotnet build` succeeds solution-wide.

## 6. Controllers

`EbayControllerImplementation` and `ProductPassportFileController` are the last call sites touching `IgnoredLot`, `ClientError`, `ProductEmailSendHistory`, and `ProductPassport` directly via `ApplicationDbContext` - none of these tables have a target-shape mapping yet.

- [x] 6.1 `ProductPassport` became its own `AggregateRoot<Guid>` in `Server.Domain` (`Create` factory, `SetOrder` domain method - it has minimal but real behavior: reordering siblings when one is deleted or moved, so a plain "mechanical copy" entity wasn't quite right; dropped the cross-aggregate `Product` navigation property, same fix shape as `ProductEmailSendHistory`/`Lot` before it), moved to `WriteModelDbContext`/`wm."ProductPassports"` via a new `MoveProductPassportToWriteModel` migration (data copy, `CreatedAt`/`ChangedAt` backfilled to `now()` since - unlike Lots/Currencies - passports never carried any date field to backfill from instead) paired with an emptied `DropLegacyProductPassportAfterMove` legacy migration; added `IProductPassportRepository`/`ProductPassportRepository`. Extended the existing `ProductPassportView`/`IPassportQueries` read-model port (already partly in place, consumed by `EbayLotDescriptionPage`) rather than adding a parallel one: `Passport` gained an `Order` field (needed by `GetProductPassportsAsync`'s existing ordering), and a new `GetPassportFileAsync` method plus `PassportFile` DTO were added for the one caller that actually needs the binary `Content`/`ContentType` (the file-download endpoint) - keeping the lightweight listing query lightweight, since EF only selects the columns a given `.Select()` projection actually references. `ProductPassportFileController` moved into `Server.Adapters.Driving.WebApi/Controllers` unchanged in shape (still a plain manual `[ApiController]`, not OpenAPI-generated - its own `TODO` already flags that as future work, untouched here since it's out of this task's scope; the `web-api` skill's contract conventions don't apply to a controller with no YAML contract). **Also updated in the same commit, since leaving them on the dead `ApplicationDbContext.ProductPassports` DbSet would have broken immediately once the table moved (same "repoint call sites now even though the class's full move is a later task" pattern as 4.1/4.2/5.1):** `EbayControllerImplementation`'s `GetProductPassportsAsync`/`UploadProductPassportAsync`/`DeleteProductPassportAsync`/`UpdateProductPassportAsync` (the last one wasn't mentioned in this task's own text but hits the exact same table) - repointed to the new repository/query port; `UpdateProductPassportAsync`'s swap-range reorder algorithm (shift siblings between old and new position by ±1, matching the original's direction logic exactly) now loads each affected aggregate individually through the repository rather than mutating a single already-tracked list from one query, an N+1 tradeoff accepted since it matches the repository-based command pattern used everywhere else in this migration and passport counts per product are small. New `Tests.Unit/ProductPassportTests.cs` covers `Create`/`SetOrder`. Verified: full solution builds; `Tests.Unit` (87/87) and `Tests.Integration` (31/31) pass against a freshly-bootstrapped, freshly-migrated database; `openspec validate --all --strict` passes; `check-openspec-test-coverage` reports 70/70. No pre-existing dedicated `ProductPassport` integration tests were found (same gap as several tasks this phase) - relied on the same build/DI-validation/migration verification, plus the new domain unit tests for the one piece of genuinely new logic (`SetOrder`).
- [x] 6.2 Moved `MeasurementPageController` (all 6 endpoints: `download`, `ebay_curves`, `ebay_tube_description`, `sold`, `curves`, `empty_picture`) into `Server.Adapters.Driving.WebApi/Controllers`, unchanged - it's a plain `[ApiController]`/`ControllerBase` with raw `[HttpGet]` route attributes, not backed by an OpenAPI contract (serves SVG/zip bytes, not JSON DTOs), same shape as the already-migrated `RobotsController`/`ExtensionController` in that same project; no `web-api` skill contract work applies here for that reason. All its port dependencies (`IMeasurementService`, `IGeoIpService`, `MeasurementPlotService`) resolved without any new `ProjectReference`, since `Server.Adapters.Driving.WebApi.csproj` already referenced `Server.Application.New` (which transitively carries `Server.Application.Abstractions.Driven`/`.Driving`). `Server.Adapters.Driving.WebApi`'s existing `AddControllers().AddApplicationPart(...)` discovery picked up the moved controller with no registration change needed. Verified: full solution builds; `Tests.Unit` (85/85) and `Tests.Integration` (31/31, including the 6 `ProductMeasurementFlowTests` that exercise several of these routes end-to-end) pass; the `[Explicit]`-tagged `Tests.Explicit/MeasurementPageTest.cs` (requires a live deployed server, not runnable in this sandbox) was left untouched since it targets the route contract, not this file.
- [x] 6.3 `ProductEmailSendHistory` was already moved in task 5.1 (pulled forward since `ChipfindBackgroundTask` needed it). `IgnoredLot` and `ClientError` moved here: `IgnoredLot` stayed a plain (non-aggregate) class per `design.md`'s "no behavior" categorization - it carries zero data beyond the `(ProductId, LotId)` composite key that already *is* its identity, so `IRepository<TAggregate,TId>`'s single-scalar-id shape doesn't fit; got a bespoke `IIgnoredLotRepository` (`InsertMissingAsync`/`RemoveAsync`, replacing the `FlexLabs.EntityFrameworkCore.Upsert` call - which only ever meant "insert if missing" here, since there is no other field to update - with an explicit existing-rows-query-then-insert-missing, dropping the `FlexLabs` package dependency entirely) and `IIgnoredLotQueries` (`GetIgnoredLotIdsAsync`/`IsLotIgnoredAsync`) for the command/query split `GetIgnoredLotsAsync`/`GetIsLotIgnoredForProductAsync` need. `ClientError` became a minimal `Entity<Guid>` (gets `CreatedAt`/`ChangedAt` audit columns for free via `WriteModelDbContext.ApplyAudit()`, which it never had before - a safe, backward-compatible addition since it's a write-only sink nothing reads back) with an `IClientErrorRepository.AddAsync`-only port (no read side needed - grepped and confirmed nothing anywhere reads `ClientError` back). Both migrated via a combined `MoveIgnoredLotAndClientErrorToWriteModel` migration (data copy; `ClientError`'s `CreatedAt`/`ChangedAt` backfilled to `now()` same as `ProductPassport`, `IgnoredLot` never had a date to backfill) paired with an emptied `DropLegacyIgnoredLotAndClientErrorAfterMove` legacy migration, same shape as every prior move this phase.

  All three of `EbayControllerImplementation`'s `TransactionScopeFactory` usages are gone: `IgnoreLotsAsync`'s was the last one standing (the other two task 1.3 counted had already been cleared by 4.1/4.2's fixes) - now that both the existence check (`ILotQueries`, read model) and the insert (`IIgnoredLotRepository`, `WriteModelDbContext`) commit through ports backed by a single write context, no ambient scope is needed. `UpsertLotInfoAsync`'s IgnoredLot cleanup (previously a *second*, separate `ApplicationDbContext.SaveChangesAsync` specifically because IgnoredLot lived on a different connection) now merges into the same single `_writeModelUnitOfWork.SaveChangesAsync` as the Lot upsert - a genuine atomicity improvement, not just a relocation. `TransactionScopeFactory.cs` itself is now fully unused (grepped, zero remaining call sites) but deliberately left in place for task 7.4 to formally delete, per the sequencing task 1.3 set up.

  Moved `EbayController`/`EbayControllerImplementation`/`LotDataToExtract`/`NonOkHttpAnswerException`/`ErrorFilter`/`ModelsExtensions`/`HtmlUtilities` into `Server.Adapters.Driving.WebApi/Controllers` verbatim (no business logic needed extracting into a separate `Server.Application.New` use-case service beyond what already lives there via `ProductService`/`ILotRepository`/etc. - `EbayControllerImplementation` itself is already a thin orchestration layer over those, consistent with how `MeasurementPageController`/`ProductPassportFileController` moved unchanged in 6.1/6.2). The NSwag `GenerateLegacyApiClients`/`IncludeGeneratedLegacyControllers` MSBuild targets moved from `Server.Application.csproj` to `Server.Adapters.Driving.WebApi.csproj` too (same exact command/output path, so `IEbayController` and the generated DTOs keep their `Server.Controllers.Generated` namespace and stay source-compatible); merged its `Compile` glob with the existing `WebApi.yaml`-driven target's, since both land in the same `obj/Generated/Controllers/` folder and a duplicate `<Compile Include>` over the same glob would double-include files. `WellKnown.Formats.TimeFormat` (the only legacy `WellKnown` member these files needed) moved into a small `WellKnown` class alongside them, since the legacy one is `internal` and thus physically unreachable once its only callers live in a different assembly. `.AddNewtonsoftJson()`/`ErrorFilter` moved into `Server.Adapters.Driving.WebApi`'s own `AddWebApiAdapter()` - confirmed this changes nothing behaviorally, since ASP.NET Core merges all `AddControllers`-style filter/formatter configuration into one process-wide `MvcOptions` regardless of which project's `IServiceCollection` extension registered it, so `ErrorFilter` was already applying to every controller in the app (including the WebApi adapter's own `RobotsController`/etc.) before this move too. `Tests.Explicit/LotsExplicitTests.cs` (an `[Explicit]` test using `HtmlUtilities` directly) needed a new `[assembly: InternalsVisibleTo("Tests.Explicit")]` on `Server.Adapters.Driving.WebApi` (the legacy grant on `Server.Application` no longer reaches it) plus a `using` repoint.

  `ApplicationDbContext` now has **zero** remaining domain `DbSet`s or entity configs beyond Identity/inbox/outbox and `Ignore<T>()` calls - confirmed by inspection, which is exactly what task 7.1 will re-verify. `Server.Application.csproj` lost `FlexLabs.EntityFrameworkCore.Upsert`/`HtmlAgilityPack`/`ScottPlot`/`alglib.net`/`Microsoft.AspNetCore.Mvc.NewtonsoftJson`/`NSwag.MSBuild` (all now unused there - grepped every remaining source file first) and both NSwag targets; `Server.Application/Controllers` and `Server.Application/Infrastructure/{HtmlUtilities,ModelsExtensions}.cs` are deleted (`Server.Application/Infrastructure` now holds only `DictionaryExtensions.cs` and the soon-to-be-deleted `TransactionScopeFactory.cs`).

  New `Tests.Integration/Tests/IgnoredLotFlowTests.cs` exercises `IgnoreLotsAsync`/`GetIgnoredLotsAsync`/`GetIsLotIgnoredForProductAsync` end-to-end through the real HTTP endpoints, including that re-ignoring an already-ignored lot doesn't duplicate-insert (the one piece of genuinely new logic replacing the `FlexLabs` upsert) - no such test existed before since `IgnoredLot` was never exercised by the existing suite. `SaveErrorAsync` (`ClientError`) has no dedicated test, same "trivial single-line write, no branching logic" reasoning applied to `SaleAdvertisementCleanupBackgroundTask` in 5.5. Verified: full solution builds (0 errors); `Tests.Unit` (87/87) and `Tests.Integration` (32/32, including the new test and the pre-existing `LotPricingFlowTests`/`ProductMeasurementFlowTests`/`AuthorizedEndpointTests`/`MeasurementPhotosFlowTests` that already exercise `EbayController` end-to-end and continued passing unchanged) pass against a freshly-bootstrapped, freshly-migrated database; `openspec validate --all --strict` passes; `check-openspec-test-coverage` reports 70/70.
- [x] 6.4 `Server.Application/Controllers` was already deleted as part of task 6.3's move (confirmed empty/absent). `dotnet build` succeeds solution-wide. Ran the app for real (`dotnet run --launch-profile Server`, not just `Tests.Integration`'s `WebApplicationFactory`) and `curl`'d a representative route from each moved controller: `GET /api/ebay/v1/products` and `/api/ebay/v1/categories` (legacy `EbayController`, `[Authorize]`) both returned `401` unauthenticated - confirming the route resolves and is routed to the moved, still-`[Authorize]`-protected controller, not a 404; `GET /products/{id}/passports/{id}` (`ProductPassportFileController`, `[AllowAnonymous]`) returned `404` from the controller's own not-found logic (no such passport), not routing failure; `GET /m/sold` and `GET /empty_picture` (`MeasurementPageController`) both returned `200`. `chrome_extensions/auth` and `/robots.txt` (untouched controllers, sanity check nothing else broke) returned `200`. Server startup log showed all 6 hosted services starting/finishing cleanly and no unexpected errors, only the expected `CS0618` obsolete-usage build warnings (pre-existing, downgraded to non-error via the repo's global `WarningsNotAsErrors`) and expected "Authorization failed" log lines from the intentionally-unauthenticated `curl` calls. Combined with `Tests.Integration`'s 32/32 (which already exercises several of these same routes through the real HTTP pipeline), every previously-legacy route is confirmed still resolving correctly.

## 7. Retire `ApplicationDbContext`, Identity, and the legacy project

By this point every domain/write table has already moved in the phase that owned it (3-6); `ApplicationDbContext` should carry only ASP.NET Identity + Duende IdentityServer operational tables plus a handful of ungrouped legacy files.

- [x] 7.1 Verified by inspection of `Server.Application/Data/ApplicationDbContext.cs`: it declares zero domain `DbSet` properties (only what `ApiAuthorizationDbContext<ApplicationUser>` provides for Identity/Duende plus MassTransit inbox/outbox via `AddInboxStateEntity`/`AddOutboxMessageEntity`/`AddOutboxStateEntity`); `OnModelCreating` explicitly `Ignore<T>()`s all 7 domain types that could otherwise be pulled in by convention (`Product`, `Lot`, `Currency`, `ProductEmailSendHistory`, `ProductPassport`, `IgnoredLot`, `ClientError` - `Purchase` is owned by `Lot` so needs no separate `Ignore`, and `ProductMeasurement`/`TubeWorkingPoint`/`MatchedPairDifference`/`CacheEntry` were already fully removed from this context in earlier phases with no lingering navigation to reintroduce them). Confirmed nothing was missed: `dotnet build` succeeds and both `ApplicationDbContext`'s and `WriteModelDbContext`'s migrations apply cleanly against a freshly-bootstrapped database (verified repeatedly throughout phases 4-6). No migration needed - this was pure verification, matching the task's own "if any were missed" framing.
- [ ] 7.2 Create a new dedicated Identity `DbContext` + migrations project (e.g. `Server.Adapters.Driven.EF.Identity`, per `design.md`'s Open Questions) hosting `ApplicationUser` and the Duende `OperationalStoreOptions` tables; migrate existing Identity data into it; verify a full login/token-refresh/logout cycle works against the new context before removing the old one (per `design.md`'s Risks).
- [x] 7.3 Ran `grep -rn "^using Server\.Application\." Server.Adapters.* Server.Application.New` (and a second pass for `using Server.Application;`/`.Data`/`.Controllers`/`.Infrastructure`/`WellKnown`/`EbayServerOptions`/`ImageCacheOptions`/`DatabaseConcurrentAccessSemaphore` specifically, in case a bare/no-subnamespace reference slipped past the first pattern). Every hit across both passes resolves to `Server.Application.Abstractions.Driven.*`, `Server.Application.Abstractions.Driving.*`, or `Server.Application.New.*` - all three legitimate, separate projects, never the legacy `Server.Application` project itself. Zero hidden legacy references remain (the `MatchedPairsCalculatorConsumer` case `design.md` flagged was already fixed in an earlier phase). No changes needed - this was pure verification.
- [x] 7.4 Confirmed all 5 `TransactionScopeFactory` call sites from task 1.3 (`EbayControllerImplementation.cs` x3, `SaleAdvertisementCleanupBackgroundTask.cs`, `ChipfindBackgroundTask.cs`, `CalculatePricesForLotConsumer.cs`) had their usage removed by phases 4-6 (grepped: zero remaining call sites before deleting). Deleted `Server.Application/Infrastructure/TransactionScopeFactory.cs`. Each of those previously cross-context operations now commits atomically through a single `WriteModelDbContext`/`IWriteModelUnitOfWork.SaveChangesAsync` call instead of an ambient `TransactionScope` spanning two connections - verified indirectly throughout phases 4-6's own integration tests (`LotPricingFlowTests`, `IgnoredLotFlowTests`, etc., all still passing) rather than one new dedicated test, since the atomicity property is inherent to the single-context design each phase already landed, not a separately-testable behavior. Verified: `dotnet build` succeeds; `Tests.Unit` (87/87) and `Tests.Integration` (32/32) pass.
- [x] 7.5 Guarded `20260222182724_BackfillLotForSaleFromMeasurements`'s two raw-SQL blocks that touch the legacy `"ProductMeasurements"` table (the backfill `DO $$` block and the FK-add `DO $$` block) with an `IF NOT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'ProductMeasurements') THEN RETURN; END IF;` early-exit at the top of each, so both degrade to a no-op once the legacy table is gone; also hardened `DROP TABLE wm."LotForSale_BackfillMap"` to `DROP TABLE IF EXISTS` for the same reason (harmless either way, since that table is created unconditionally earlier in the same migration). Editing this already-applied migration's `Up()` is safe per `design.md`'s reasoning (EF tracks applied migrations by name in `__EFMigrationsHistory`, not by re-checksumming the file, so this only changes behavior for a database that has never run it). Verified: full solution builds; `Tests.Integration` (32/32) passes against a freshly-bootstrapped database - though since `Server.Application/Migrations` (and the legacy `ProductMeasurements` table it creates) isn't deleted until task 7.6, this run only confirms the guard is inert/correct while the table still exists; the guard's actual payoff (a fresh bootstrap with no legacy table at all) gets exercised for real by 7.6's own fresh-DB verification, which this task deliberately precedes.
- [ ] 7.6 Delete `Server.Application/Data/ApplicationDbContext.cs`, `ApplicationUser.cs`, `Server.Application/Migrations`, `Server.Application/Areas/Identity`, `WellKnown.cs`, `ServiceCollectionExtensions.cs`, `DatabaseConcurrentAccessSemaphore.cs`, `EbayServerOptions.cs`, `ImageCacheOptions.cs`, and finally the empty `Server.Application` project itself; remove its reference from `Server.csproj`/`Ebay.slnx`; verify `dotnet build` succeeds for the whole solution with no reference to `Server.Application` remaining anywhere, and the integration test suite still bootstraps a fresh database successfully.

## 8. Documentation and final validation

- [ ] 8.1 Update `src/Ebay/AGENTS.md`'s "Module structure" and "DB migrations" sections to remove references to the now-deleted legacy `Server.Application`/`ApplicationDbContext` (module structure entry, legacy migrations command, legacy contract note if still applicable).
- [ ] 8.2 Run `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` and fix any missing/stale `[OpenSpecScenario]` mappings surfaced by moved code.
- [ ] 8.3 Run `./scripts/agent-check/agent-check.sh` from the repository root and confirm it passes end to end (build, tests, OpenSpec validation).
