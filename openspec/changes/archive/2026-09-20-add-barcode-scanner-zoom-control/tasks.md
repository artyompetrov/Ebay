## 1. Zoom control in the shared scanner interop

- [x] 1.1 In `StartQrScanner()` (`Frontend/wwwroot/js/interop.js`), after the camera starts, read the running track's capabilities (`html5QrCode.getRunningTrackCapabilities()`) and detect whether a `zoom` range is reported; verify with a unit test that no DOM changes happen when the capability is absent.
- [x] 1.2 When a `zoom` capability is present, create and inject a range-slider element into the DOM next to `#reader`, bounded by the capability's `min`/`max`/`step`; verify with a unit test that the slider is created with those bounds.
- [x] 1.3 Wire the slider's `input` event directly to `html5QrCode.applyVideoConstraints({ advanced: [{ zoom: value }] })` (no Blazor interop round trip); verify with a unit test that dragging the mocked slider calls `applyVideoConstraints` with the expected zoom value.
- [x] 1.4 Remove the injected slider when scanning stops (on successful decode or on error) so it doesn't linger after the camera closes; verify with a unit test that the slider element is gone after `stop()`/`clear()` resolve.
- [x] 1.5 Add minimal CSS for the slider's placement under the scanner preview; verify visually that it doesn't overlap the `#reader` video on both `MeasurementPhotos.razor` and `Measurements.razor` (which have their own, separate `#reader` style blocks).

## 2. Tests

- [x] 2.1 Extend the `Html5Qrcode` mock in `Frontend/Tests/measurement-photos-interop.test.mjs` with `getRunningTrackCapabilities` and `applyVideoConstraints`, configurable per test case to report zoom support or not.
- [x] 2.2 Add a test that the zoom slider is shown when the mocked camera reports a zoom capability, tagged `[OpenSpecScenario("measurement-photos", "Phone-based photo upload", "Zoom control shown when the camera supports zoom")]`.
- [x] 2.3 Add a test that no zoom slider is shown when the mocked camera does not report a zoom capability and existing scan behavior (resolve, stop, clear) is unaffected, tagged `[OpenSpecScenario("measurement-photos", "Phone-based photo upload", "Zoom control absent when the camera does not support zoom")]`.
- [x] 2.4 Add a test that adjusting the slider applies the corresponding zoom value to the running track without restarting the camera, tagged `[OpenSpecScenario("measurement-photos", "Phone-based photo upload", "Adjusting the zoom control changes the live preview")]`.
- [x] 2.5 Run the existing `StartQrScanner` and orientation tests unchanged and confirm they still pass, proving the zoom addition doesn't alter mobile/non-mobile mirroring or the resolved scan value.

## 3. Verification

- [~] 3.1 Manually confirm (per `AGENTS.md`'s local backend debugging checks) that the zoom slider also appears and works on `Measurements.razor`'s barcode-to-upload scan flow, since it shares `StartQrScanner()` with `MeasurementPhotos.razor` - no code change expected there beyond what task 1 already touches. **Not run**: requires a real phone with a zoom-capable camera; this dev environment has no camera hardware or browser+camera harness to exercise it. Needs a human with a phone to verify before merge.
- [~] 3.2 Run `./scripts/check-openspec-test-coverage/check-openspec-test-coverage.sh` and fix any missing/stale `[OpenSpecScenario]` mappings for the `measurement-photos` capability. **Blocked in this environment**: the script's `execFileSync('openspec', ...)` fails with `spawnSync openspec ENOENT` under Windows/git-bash here (the CLI itself works fine when invoked directly - confirmed via `openspec --version`), a pre-existing environment gap unrelated to this change. Manually verified instead: grepped the repo for every `[OpenSpecScenario("measurement-photos", "Phone-based photo upload", ...)]` tag - all 8 scenario names (5 pre-existing + the 3 new zoom ones) have at least one matching test tag across `MeasurementPhotosTests.cs` and `measurement-photos-interop.test.mjs`. Should still be re-run for real on Linux/CI before merge.
- [~] 3.3 Run `./scripts/agent-check/agent-check.sh` from the repository root and confirm it passes end to end. **Not run in full**: its last step reuses the same blocked coverage script (task 3.2), and its `dotnet test`/Chrome-extension steps weren't exercised here. Verified the parts relevant to this change directly instead: `dotnet build` (solution-wide) succeeded, `node --test Frontend/Tests/*.test.mjs` passed (9/9, including the new zoom tests), and `openspec validate --all --strict` / `openspec validate --archived` both passed. Full `agent-check.sh` should still be run for real (CI or a Linux/WSL shell) before merge.
